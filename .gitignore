# Secrets - never commit, even to a private repo
.env
.env.*
**/*.key
**/*.pem
**/secrets*
**/*credentials*

# OS noise
.DS_Store
Thumbs.db

# Temporary onboarding file, deleted once the birth sequence completes
BOOTSTRAP.md

# Nested agent workspace with its own .git - back it up as a separate repo
findmyshotsstudio/
