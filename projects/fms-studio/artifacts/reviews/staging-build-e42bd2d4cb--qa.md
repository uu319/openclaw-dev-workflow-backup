APPROVED
Reviewed SHA: 683599db4d47bb9384b7697fdbcbba732084f5b9

Fix content-type lockfile resolution: implemented in package.json and package-lock.json diff (content-type moved to standard dependencies).

## Non-blocking
The PR diff also includes some nested Tailwind oxide WASM dependencies in the lockfile that appear to be generated by `npm ci`/`npm install`. This is fine and expected when updating the lockfile, but noted for completeness.